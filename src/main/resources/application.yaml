server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: awss3
  cloud:
    config:
      server:
        awss3:
          region: ap-southeast-1
          bucket: **removed bucket name**

eureka:
  client:
    enabled: true
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
